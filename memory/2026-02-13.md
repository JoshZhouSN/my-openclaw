# 2026-02-13 日志

## 系统维护 - Update Plus 备份机制修复

### 问题发现
- 检查 Update Plus 备份机制时发现之前的定时任务（每天4点 `update-plus update`）在 2026-02-12 后中断
- 符号链接指向错误路径：`~/bin/update-plus` -> `~/.openclaw/skills/update-plus/bin/update-plus`
- 实际路径应为：`~/clawd/skills/update-plus/bin/update-plus`

### 修复内容
1. **修复符号链接**
   ```bash
   rm ~/bin/update-plus
   ln -s /home/ubuntu/clawd/skills/update-plus/bin/update-plus ~/bin/update-plus
   ```

2. **创建新的备份脚本**
   - 路径：`/home/ubuntu/clawd/scripts/healthchecks/daily-backup.sh`
   - 功能：运行 `update-plus backup` + Healthchecks 监控
   - 不执行 update（避免 bun 缺失错误）

3. **配置 Cron 定时任务**
   - 时间：每天 04:00（北京时间）
   - 命令：`TZ=Asia/Shanghai /home/ubuntu/clawd/scripts/healthchecks/daily-backup.sh`
   - 监控：成功/失败都会上报 Healthchecks.io

### 备份配置
- 备份目录：`~/.openclaw/backups/`
- 备份内容：`~/.openclaw` (config) + `~/clawd` (workspace)
- 保留数量：最近 10 个备份
- 日志：`~/.openclaw/backups/cron-backup.log`

### 测试结果
- 手动测试备份成功，生成 158MB 备份文件
- 下次自动备份：明天凌晨 4:00（北京时间）
